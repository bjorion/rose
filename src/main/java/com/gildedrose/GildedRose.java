package com.gildedrose;

import com.gildedrose.model.AbstractItem;

import java.util.Arrays;

class GildedRose {

    @Deprecated
    Item[] items;

    public GildedRose(Item[] items) {

        this.items = items;
    }

    public void updateQuality2() {

        // Convert Item to AbstractItem
        var newItems = Arrays.stream(items).map(AbstractItemFactory::convert).toList();

        // Execute the business logic
        newItems.forEach(AbstractItem::updateQuality);

        // Convert AbstractItem to Item
        var items = new Item[newItems.size()];
        for (int i = 0; i < newItems.size(); i++) {
            var newItem = newItems.get(i);
            items[i] = new Item(newItem.getName(), newItem.getSellIn(), newItem.getQuality());
        }
        this.items = items;
    }

    @Deprecated
    public void updateQuality() {
        for (int i = 0; i < items.length; i++) {
            if (!items[i].name.equals("Aged Brie")
                    && !items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                if (items[i].quality > 0) {
                    if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                        items[i].quality = items[i].quality - 1;
                    }
                }
            } else {
                if (items[i].quality < 50) {
                    items[i].quality = items[i].quality + 1;

                    if (items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                        if (items[i].sellIn < 11) {
                            if (items[i].quality < 50) {
                                items[i].quality = items[i].quality + 1;
                            }
                        }

                        if (items[i].sellIn < 6) {
                            if (items[i].quality < 50) {
                                items[i].quality = items[i].quality + 1;
                            }
                        }
                    }
                }
            }

            if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                items[i].sellIn = items[i].sellIn - 1;
            }

            if (items[i].sellIn < 0) {
                if (!items[i].name.equals("Aged Brie")) {
                    if (!items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                        if (items[i].quality > 0) {
                            if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                                items[i].quality = items[i].quality - 1;
                            }
                        }
                    } else {
                        items[i].quality = items[i].quality - items[i].quality;
                    }
                } else {
                    if (items[i].quality < 50) {
                        items[i].quality = items[i].quality + 1;
                    }
                }
            }
        }
    }
}
